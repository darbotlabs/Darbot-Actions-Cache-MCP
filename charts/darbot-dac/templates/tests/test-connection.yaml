apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dar-act-cache.fullname" . }}-test-connection"
  labels:
    {{- include "dar-act-cache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dar-act-cache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

